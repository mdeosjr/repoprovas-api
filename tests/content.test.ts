import supertest from 'supertest';
import server from '../src/index.js';
import startTest from './startTest.js';

describe('GET /content/teachers', () => {
	startTest();

	it.todo('should return 200 and a array');
});

describe('GET /content/terms', () => {
	startTest();

	it.todo('should return 200 and a array');
});